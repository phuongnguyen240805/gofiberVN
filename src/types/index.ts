import { RouterOutputs } from '@/utils/api';

// TODO: Add collection and product routers
export type Collections = any; // RouterOutputs['collection']['all'] | undefined;

export type Product = any; // RouterOutputs['product']['all']['products'][0];
